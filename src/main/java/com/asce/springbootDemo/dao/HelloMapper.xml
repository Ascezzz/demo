<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asce.springbootDemo.dao.HelloMapper">
    <insert id="insertUser" parameterType="com.asce.springbootDemo.controller.req.InsertReq">
        insert into user
        set
        <if test="null != userName">
            user_name = #{userName},
        </if>
        <if test="null != account">
            account = #{account},
        </if>
        <if test="null != password">
            password = #{password}
        </if>
    </insert>
</mapper>